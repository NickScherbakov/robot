# Jekyll Configuration for Earning Robot Website

# Site settings
title: "Earning Robot"
description: "Autonomous earning robot — privacy, ethics, profit"
baseurl: "/robot"
url: "https://nickscherbakov.github.io"

# Build settings
markdown: kramdown
# theme: minima
# plugins:
#   - jekyll-feed
#   - jekyll-seo-tag

# Exclude from processing
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor

# Collections
collections:
  sections:
    output: false

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: "default"
